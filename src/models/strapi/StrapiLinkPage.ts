import Locale from '../Locale';

interface StrapiLinkPage {
  title: string;
  slug: string;
  locale: Locale;
  publishedAt: string;
  createdAt: string;
  updatedAt: string;
}

export default StrapiLinkPage;
